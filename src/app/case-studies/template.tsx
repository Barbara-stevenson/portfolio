/**
 * Intentionally empty — transition is now handled by layout.tsx
 * and SlideTransitionProvider.
 */
export default function CaseStudyTemplate({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
